
ON MAC SYSTEMS:
1. On a new mac. Install home-brew. Then using brew install git, python, vim (newer version)\
2. install tmux via brew. install TPM (tmux plugin manager) copy config files.\
3. track all the bin files all the dot files.\


ON UBUNTU SYSTEMS:\
use the installing ubuntu_18.04.txt on dropbox
